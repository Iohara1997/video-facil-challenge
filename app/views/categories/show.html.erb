<p id="notice"><%= notice %></p>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <td>Título</td>
            <td>URL</td>
            <td>Categoria</td>
            <td colspan="2">
        </tr>
    </thead>
    <tbody>
        <%= render @videos_category %>
    </tbody>
</table>

<%= link_to 'Editar', edit_category_path(@category), class: "btn btn-info" %> |
<%= link_to 'Voltar', categories_path, class: "btn btn-primary" %>
